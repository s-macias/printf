_printf(3) | Documentation


NAME
====
_printf

**_printf**  prints formatted text

SYNOPSIS
========

#include <stdio.h>
#include <stdarg.h>
| int _printf(const char *format, ...)


DESCRIPTION
===========

The function prints formatted text to the standard output. It prints different
kinds of output depending on a conversion specifier defined by the user.

FLAG CHARACTERS
===============

The conversion specifiers are defined by a % and a character:
c to print a character
s to print a string
% to print the % symbol
d to print a decimal
i to print an integer

RETURN VALUE
============

It returns an integer that represents the number of characters printed.

EXAMPLE
=======

printf("%d", 7);

Output: 7
Return: 1


This line of code would print the integer n; we know it is an integer because %d
is the conversion specifier for integers.

FILES
=====

*_printf.c*

*holberton.h*

AUTHORS
=======
Eleanny Garcia and Sandra Macias

18/03/2020